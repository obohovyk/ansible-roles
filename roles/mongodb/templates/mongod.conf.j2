# {{ ansible_managed }}

# for documentation of all options, see http://docs.mongodb.org/manual/reference/configuration-options

# where to write logging data
systemLog:
  destination: file
  logAppend: true
  path: /var/log/mongodb/mongod.log

# where and how to store data
storage:
  dbPath: {{ mongodb_path }}
  journal:
    enabled: true
#  engine:
#  mmapv1:
#  wiredTiger:

# how the process runs
processManagement:
  fork: true
  pidFilePath: {{ mongodb_pidfile }}

# network interfaces
net:
  port: {{ mongodb_port }}
  bindIp: {{ mongodb_bind_address|join(',') }}

#security:

#operationProfiling:

#replication:

#sharding:

## Enterprise-Only Options

#auditLog:

#snmp:
